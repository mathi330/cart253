/**
I like to move it!
Mathilde Davan

This is a project where blah blah blah...
*/

"use strict";

let backgroundShade = 0;

//JavaScript object!!!
let circle = {
  x: 0,
  y: 350,
  size: 200,
  speed: 2,
  fill: 255,
};

/**
Description of preload
*/
function preload() {}

/**
Description of setup
*/
function setup() {
  createCanvas(windowWidth, windowHeight);
}

/**
Description of draw()
*/
function draw() {
  // console.log(
  //   `circle.x: ${circle.x}, circle.y: ${circle.y}, circle.size: ${circle.size}, circle.speed: ${circle.speed}`
  // );
}
